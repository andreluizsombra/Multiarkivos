@using MK.Easydoc.Core.Entities;
@{
    ViewBag.Title = "Lista de Relatórios";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="panel-heading">
    <h4 class="panel-title"><i class="fa fa-list"></i>&nbsp;@ViewBag.Title</h4>
</div>
<div class="container-fluid">
    <div class="row">
        <div class="col-lg-12">
            <hr />
            <div class="table-responsive">
                <table id="tblUsuarios" class="table table-striped table-hover table-condensed">
                    <thead>
                        <tr>
                            <th>Titulo</th>
                            <th>Descrição</th>
                            <th>Relatório</th>
                        </tr>
                    </thead>
                    @if (ViewBag.Lista != null)
                    {
                        foreach (var lst in ViewBag.Lista)
                        {
                            <tr id="lin_@lst.idRelatorio">
                                <td>@lst.Titulo</td>
                                <td>@lst.Descricao</td>
                                <td><a href="@Url.Content("~/Relatorio/Gerar/")@lst.idRelatorio" class="usu"><i class="fa fa-list fa-lg" data-toggle="tooltip" title="Gerar Relatório"></i></a></td>
                            </tr>
                        }
                    }
                </table>
            </div>
        </div>
    </div>
    <br />
</div>

